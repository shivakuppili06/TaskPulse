{
  "name": "apextask",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix frontend",
    "dev": "npm run dev --prefix frontend"
  },
  "dependencies": {
    "firebase": "^12.15.0"
  }
}
